<h5 class="mb-0 fw-bold">

    Extra Charges
</h5>


<div class="d-flex justify-content-end mb-3">
    <button type="button" class="btn btn-sta" id="add-another-charge">Add Another Extra
        Charge
    </button>
</div>
<div id="extra-charge-container">
    <div class="row extra-charge-container">
        <!-- <div class="form-group col-md-6">
            <label for="charge_name">Extra Charge Name</label>
            <select class="form-control extra_charge" name="chargeid[]" id="charge_name">
                <option value="" selected hidden>Select Extra Charge</option>
                {{--@foreach ($charges as $charge)
                    <option value="{{$charge->id}}" data-value="{{$charge->charge_fee}}"
                        data-frequency="{{$charge->frequency}}">
                        {{ $charge->charge_name }}
                    </option>
                @endforeach--}}
            </select>
        </div>
        <div class="form-group col-md-6">
            <label for="extra_value">Extra Charge Value</label>
            <input type="text" class="form-control" id="extra_value" name="chargeValue[]"
                placeholder="Extra Charge Value"/>
        </div>
        <div class="form-group col-md-6">
            <label for="extra_frequency">Frequency</label>
            <input type="text" class="form-control" name="chargefrequency[]" id="extra_frequency" readonly>
        </div> -->
    </div>
</div>